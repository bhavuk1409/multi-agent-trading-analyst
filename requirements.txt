# NEXUS — Multi-Agent Trading Analyst: Lightweight Python dependencies for Vercel
# ==============================================================================

# Market data (Yahoo Finance)
yfinance>=0.2.28

# Data processing & utilities
numpy>=1.24.0
pandas>=2.0.0
pyyaml>=6.0
python-dotenv>=1.0.0

# LLM & Multi-Agent (Groq OpenAI-compatible endpoint) — lightweight <15MB
openai>=1.0.0

# News search (optional — falls back to yfinance headlines)
exa-py>=1.15.6