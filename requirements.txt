# NEXUS — Multi-Agent Trading Analyst: Python dependencies
# =========================================================
# Install with:
#   pip install -r requirements.txt -c constraints.txt

# ---------------------------------------------------------------------------
# Market data  (no API key required)
# ---------------------------------------------------------------------------
yfinance>=0.2.28

# ---------------------------------------------------------------------------
# Data processing
# ---------------------------------------------------------------------------
numpy>=1.24.0
pandas>=2.0.0
pyyaml>=6.0
python-dotenv>=1.0.0

# ---------------------------------------------------------------------------
# LLM & Multi-Agent (Groq + LangChain)
# ---------------------------------------------------------------------------
langchain>=0.1.0
langchain-openai>=0.0.5
langchain-core>=0.3.0
openai>=1.0.0

# ---------------------------------------------------------------------------
# News search  (optional — falls back to yfinance headlines if key not set)
# ---------------------------------------------------------------------------
exa-py>=1.15.6